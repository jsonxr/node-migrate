#!/usr/bin/env node

var commandline = require('../lib/commandline');

commandline(process.argv, function (err, retcode) {
    if (err) {
        console.log(err);
    }
    // i chose the ending, not commander.js...
    process.exit(retcode);
});
